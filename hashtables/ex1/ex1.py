def get_indices_of_item_weights(weights, length, limit):
    """
    YOUR CODE HERE
    """
    # Your code here

    return None


"""
- a f() that adds 2 indeces together & compares them to the limit to find the 
- pair that equal it. 
- returns the pairs with the higher value index listed first

first pass solution:

weight = {}
results = {}  
    for i in range(length(weights):
        for j > i in range(length(weights)):
            if weights[i] + weights[j] == limit:
                results.append(weights[i][j])
            else: return None
"""
